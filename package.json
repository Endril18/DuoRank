{
  "name": "duorank",
  "version": "1.0.0",
  "scripts": {
    "start": "node dist/server.js --prefix backend",
    "dev": "npm run dev --prefix backend",
    "build": "tsc --prefix backend"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
